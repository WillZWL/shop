CREATE TABLE `display_banner_config` (
  `id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `display_id` bigint(20) unsigned NOT NULL,
  `usage` varchar(2) DEFAULT 'PV' COMMENT 'PV-Preview, PB- Published',
  `country_id` char(2) DEFAULT NULL,
  `lang_id` varchar(16) NOT NULL COMMENT 'e.g. zh-cn, zh-tw, en and etc.',
  `position_id` tinyint(3) unsigned NOT NULL DEFAULT '0',
  `banner_type` varchar(5) NOT NULL DEFAULT 'I' COMMENT 'I - Image, F - Flash, R - Rowing Banner',
  `height` int(3) unsigned NOT NULL,
  `width` int(3) unsigned NOT NULL,
  `status` tinyint(2) unsigned NOT NULL DEFAULT '1' COMMENT '0 = Inactive / 1 = Active',
  `create_on` timestamp NOT NULL DEFAULT '0000-00-00 00:00:00',
  `create_at` int(10) unsigned NOT NULL DEFAULT '2130706433' COMMENT 'IP address, default 127.0.0.1',
  `create_by` varchar(32) NOT NULL DEFAULT 'system',
  `modify_on` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `modify_at` int(10) unsigned NOT NULL DEFAULT '2130706433' COMMENT 'IP address',
  `modify_by` varchar(32) NOT NULL DEFAULT 'system',
  PRIMARY KEY (`id`),
  UNIQUE KEY `idx_display_banner_config` (`display_id`,`position_id`,`country_id`,`lang_id`,`usage`),
  KEY `fk_dbc_cid` (`country_id`),
  KEY `fk_dbc_lid` (`lang_id`),
  CONSTRAINT `fk_dbc_cid` FOREIGN KEY (`country_id`) REFERENCES `country_copy` (`id`),
  CONSTRAINT `fk_dbc_did` FOREIGN KEY (`display_id`) REFERENCES `display` (`id`),
  CONSTRAINT `fk_dbc_lid` FOREIGN KEY (`lang_id`) REFERENCES `language_copy` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8