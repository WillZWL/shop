CREATE TABLE `interface_so` (
  `trans_id` bigint(20) unsigned NOT NULL AUTO_INCREMENT,
  `batch_id` bigint(20) NOT NULL,
  `so_no` char(8) DEFAULT NULL,
  `platform_order_id` varchar(100) NOT NULL,
  `platform_id` varchar(7) NOT NULL,
  `txn_id` varchar(100) DEFAULT NULL COMMENT 'Transaction id',
  `client_id` bigint(20) unsigned DEFAULT NULL,
  `client_trans_id` bigint(20) unsigned DEFAULT NULL,
  `biz_type` varchar(20) NOT NULL COMMENT 'ONLINE/AMAZON/WALKIN/SPECIAL/OFFLINE/B2B',
  `amount` double(15,2) unsigned NOT NULL,
  `cost` double(15,2) unsigned NOT NULL,
  `vat_percent` double(5,2) unsigned NOT NULL,
  `rate` double(11,6) unsigned NOT NULL DEFAULT '1.000000',
  `ref_1` double(11,6) unsigned NOT NULL DEFAULT '1.000000',
  `delivery_charge` double(8,2) unsigned NOT NULL,
  `delivery_type_id` varchar(16) NOT NULL,
  `weight` double(8,2) unsigned NOT NULL,
  `currency_id` char(3) NOT NULL,
  `lang_id` varchar(16) NOT NULL DEFAULT 'en' COMMENT 'e.g. zh-cn, zh-tw, en and etc.',
  `bill_name` varchar(101) DEFAULT NULL COMMENT 'Default is client name',
  `bill_company` varchar(50) DEFAULT NULL,
  `bill_address` text,
  `bill_postcode` varchar(16) DEFAULT NULL,
  `bill_city` varchar(80) DEFAULT NULL,
  `bill_state` varchar(80) DEFAULT NULL,
  `bill_country_id` char(2) DEFAULT NULL,
  `delivery_name` varchar(101) DEFAULT NULL COMMENT 'Default is client name, Amazon has a separate ship to name',
  `delivery_company` varchar(50) DEFAULT NULL,
  `delivery_address` text,
  `delivery_postcode` varchar(16) DEFAULT NULL,
  `delivery_city` varchar(80) DEFAULT NULL,
  `delivery_state` varchar(80) DEFAULT NULL,
  `delivery_country_id` char(2) DEFAULT NULL,
  `status` tinyint(2) unsigned NOT NULL DEFAULT '1' COMMENT '0 = Inactive / 1 = New / 2 = Paid / 3 = Credit Checked / 4 = Partial Allocated / 5 = Full Allocated / 6 = Shipped',
  `refund_status` tinyint(2) unsigned NOT NULL DEFAULT '0' COMMENT '0 = No / 1 = Requested / 2 = Logistic Approved / 3 = CS Approved / 4 = Refunded',
  `hold_status` tinyint(2) unsigned NOT NULL DEFAULT '0' COMMENT '0 = No / 1 = Requested / 2 = Manager Requested / 3 = APS',
  `promotion_code` varchar(20) DEFAULT NULL COMMENT 'System Actual Promotion Code',
  `client_promotion_code` varchar(255) DEFAULT NULL COMMENT 'Promotion code which inputed by client',
  `expect_delivery_date` datetime DEFAULT NULL,
  `order_create_date` datetime NOT NULL,
  `dispatch_date` datetime DEFAULT NULL,
  `batch_status` varchar(2) NOT NULL DEFAULT '' COMMENT 'N = New / R = Ready update to master / S = Success / F = Failed / I = Investigated',
  `failed_reason` text,
  `create_on` timestamp NOT NULL DEFAULT '0000-00-00 00:00:00',
  `create_at` int(10) unsigned NOT NULL DEFAULT '2130706433' COMMENT 'IP address, default 127.0.0.1',
  `create_by` varchar(32) NOT NULL DEFAULT 'system',
  `modify_on` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `modify_at` int(10) unsigned NOT NULL DEFAULT '2130706433' COMMENT 'IP address',
  `modify_by` varchar(32) NOT NULL DEFAULT 'system',
  PRIMARY KEY (`trans_id`),
  KEY `client_id` (`client_id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8 ROW_FORMAT=COMPACT